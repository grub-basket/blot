{{count}} cancelled account{{^singular}}s{{/singular}} due for deletion

The following cancelled user accounts have passed the grace period and are due to be removed:

{{#users}}
- {{email}} (subscription expired {{subscriptionExpiredOn}})
{{/users}}

To remove these users, run:

```
ssh blot
login
node scripts/user/delete-cancelled-after-grace.js -fast
```
